# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Saved game-setup configs (menu Save/Load)
saves/

# Hand-authored maps (the map creator's Open/Save)
maps/

# Recorded match logs (settings + per-turn orders; resume-on-boot reads these)
games/

# pygbag web build output (rebuild with tools/build_web.sh)
/web/

# webstore key/value file (personal bests; localStorage on the web build)
/kv.json

# Claude generated feature spec/session notes
/notes

# Bot-tuning sweep ledger (tools/sweep.py)
results/
