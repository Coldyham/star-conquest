# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Buildozer / python-for-android build artefacts (Android APK build)
.buildozer/
bin/

# Saved game-setup configs (menu Save/Load)
saves/

# Recorded match logs (settings + per-turn orders; resume-on-boot reads these)
games/

# User-supplied custom AI strategies (drop-in decision functions); keep the docs
models/*
!models/README.md
